import { UserDisplayUnitsPipe } from './user-display-units.pipe';

describe('UserDisplayUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new UserDisplayUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
