import { BuildDurationPipe } from './build-duration.pipe';

describe('BuildDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
