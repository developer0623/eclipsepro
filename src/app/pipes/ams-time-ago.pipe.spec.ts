import { AmsTimeAgoPipe } from './ams-time-ago.pipe';

describe('AmsTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new AmsTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
