import { TaskTimeAgoPipe } from './task-time-ago.pipe';

describe('TaskTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
