import { SummaryUnitPipe } from './summary-unit.pipe';

describe('SummaryUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
