import { TaskActiveAgoPipe } from './task-active-ago.pipe';

describe('TaskActiveAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskActiveAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
